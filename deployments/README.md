# Deployments

Deployment addresses are kept outside application source so a redeploy does not require a code change.

After a Studio deployment, update `studionet.json` or set `VITE_CONTRACT_ADDRESS` in the environment.
