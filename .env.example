# Copy to .env for local overrides. Every value has a working default, so
# this file is optional — the app builds and deploys with no env vars at all.
# The address below is the live Studionet deployment (deployments/studionet.json).
VITE_CONTRACT_ADDRESS=0x190346355DEBf21D19eFFf69c21A0d0C96C23241
VITE_NETWORK=studionet
VITE_CHAIN_ID=61999
VITE_RPC_URL=https://studio.genlayer.com/api
VITE_EXPLORER_URL=https://explorer-studio.genlayer.com
